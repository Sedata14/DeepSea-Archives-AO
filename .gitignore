# Dépendances Node.js (Lourd et inutile à commit)
node_modules/
npm-debug.log
yarn-error.log

# Variables d'environnement et sécurité (CRITIQUE : Ne jamais commit)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Dossiers de build (si utilisation de TypeScript ou build tools)
dist/
build/

# Fichiers système d'exploitation
.DS_Store
Thumbs.db
.idea/
.vscode/

# Fichiers de couverture de tests (pour le Niveau 20)
coverage/
.nyc_output/

# Logs divers
logs/
*.log
pids/
*.pid
*.seed
*.pid.lock

# Docker (optionnel, selon configuration)
.docker/

# Prisma (si le client est généré localement, parfois utile de l'ignorer selon la config)
# Mais généralement on garde le dossier prisma/ sauf les migrations générées si elles conflictuent
# generated/